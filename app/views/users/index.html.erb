<div class="row">
	<div class="span12">
	
		<h1>Listando Usuários</h1>
		
		<table class="table table-striped table-bordered table-hover">
		  <tr>
		    <th>Name</th>
		    <th>Email</th>
		    <th></th>
		    <th></th>
		    <th></th>
		  </tr>
		
		<% @users.each do |user| %>
		  <tr>
		    <td><%= user.name %></td>
		    <td><%= user.email %></td>
		    <td><%= link_to 'Mostrar', user %></td>
		    <td><%= link_to 'Editar', edit_user_path(user) %></td>
		    <td><%= link_to 'Apagar', user, method: :delete, data: { confirm: 'Are you sure?' } %></td>
		  </tr>
		<% end %>
		</table>	
	
		<table class="table table-striped table-bordered table-hover">
			<tr>
				<td><%= link_to 'New User', new_user_path %></td>
			</tr>
		
		</table>
	
	</div>

</div>
